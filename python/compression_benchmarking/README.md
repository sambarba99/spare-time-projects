## Compression function benchmarking

For `bz2`, `gzip`, `lzma`, `zlib`, `zstd`

String compression test:

<p align="center">
	<img src="string_compression.png"/>
</p>

File (model.pth) compression test:

<p align="center">
	<img src="file_compression.png"/>
</p>
